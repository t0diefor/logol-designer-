# Loaded by `npm run build:static` (vite --mode static).
#
# Hash routing, for hosting the built app on a static server that cannot
# rewrite unknown paths to index.html. Without it, reloading on /characters
# returns a 404 from the host before the app ever runs.
VITE_ROUTER=hash
